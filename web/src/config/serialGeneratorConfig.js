app.config((serialGeneratorProvider, env) => {
  // console.log(serialGeneratorProvider.getBaseUrl());
  // serialGeneratorProvider.setBaseUrl(env.API_GITHUB_USERS);
  // console.log(serialGeneratorProvider.getBaseUrl());
});
